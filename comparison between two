#include<stdio.h>

int main(){
  int a,b;
  int max,min;

  printf("enter two numbers:");
  scanf("%d%d",&a,&b);

  max=((a+b)+abs(a-b))/2;
  min=((a+b)-abs(a-b))/2;

  printf("maximum numbers is %d\n",max);
  printf("minimum number is %d",min);
  
  return 0;
}
