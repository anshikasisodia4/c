#include<stdio.h>
#include<math.h>

int main(){
int a,b;

printf("enter two numbers:");
scanf("%d %d",&a,&b);

if(!(a^b)){
  printf("numbers are equal\n");
}else{
  printf("numbers are not equal\n");
}
 
 return 0;
}
