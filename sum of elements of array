#include<stdio.h>

int main(){
  int  n,a[10],sum=0;

  printf("enter n:");
  scanf("%d",&n);
  printf("enter elements of array:");

  for(int i=0;i<n;i++){
    scanf("%d",&a[i]);
    sum=sum+a[i];
  }
printf("sum of elements of array is :%d",sum);

  return 0;
}
